# LagZero — 给"电脑又卡了"装一个黑匣子

## 这个项目在做什么

你正在打游戏，画面突然一卡。你打开任务管理器，CPU 8%、内存 60%，一切正常——因为卡顿已经过去了，任务管理器只显示"现在"，不显示"刚才"。

LagZero 就是用来补这个缺口的。它在后台持续记录帧时间、CPU、内存、显存、窗口响应性等数据，当卡顿发生时，它不只告诉你"卡了"，还告诉你：

- 这次卡了多久、严重到什么程度
- 更像是 CPU、GPU、内存、上屏链路、后台进程还是 IO 在作怪
- 卡顿前 5 秒系统处于什么状态
- 峰值时刻哪个进程在抢资源
- 你可以优先从哪里排查

## 它给日常生活打了什么"补丁"

### 补丁一：从"卡了"到"为什么卡"

任务管理器和大多数监控工具只回答"现在发生了什么"。LagZero 回答的是"刚才那一下到底怎么回事"。它会在卡顿触发时回溯前 5 秒的完整上下文，做归因分析，把结论用中文写成一段你能看懂的话，而不是甩给你一堆数字。

### 补丁二：不是所有波动都值得报警

很多监控工具的阈值是写死的，空闲机器学出 CPU 均值 8%，游戏一开到 30% 就狂报"卡顿"。LagZero 会先花约 60 秒学习你这台机器的正常基线，之后用"偏离基线多少倍"来判断，而不是拿一个固定数字硬套。240Hz 显示器上的 8ms 和 30fps 游戏上的 8ms 含义完全不同，程序会按帧率自适应。

### 补丁三：把"卡了"分成三个等级，而不是一锅端

- **卡顿报告**：确实卡了，值得认真看原因。
- **轻微干扰**：有波动，但更像偶发抖动，不一定需要处理。
- **系统压力**：资源偏紧，还没爆成重卡顿，但正在逼近问题。

这样你不会因为切了一下窗口就被弹一堆重警告，也不会在内存快满时毫无察觉。

### 补丁四：同一类报告不会反复刷屏

如果 GPU 连续卡了 10 秒，你不需要每秒都被弹一次"GPU 瓶颈"。LagZero 对每类报告都有指数退却：第一次立刻报，之后间隔越来越长（2s → 4s → 8s → …），直到问题恢复才重置。不同类别还有不同的节奏——明显卡顿报得快，系统压力报得慢。

### 补丁五：进程 CPU 不再被误报

Windows 上 psutil 报的进程 CPU 是"单核归一化"——32 线程机器上一个占 200% 的游戏其实是整机 6%，完全正常。旧代码直接拿 200% 和固定阈值比，会把正常游戏报成"占满 CPU 导致卡顿"。LagZero 把所有进程级阈值都转成整机占比后再比较。

### 补丁六：PID 0 不再假装在捣乱

`System Idle Process`（PID 0）在空闲时报告 ~100% CPU，因为它计的就是"空转"。代码不过滤它，空闲机器会被报成"System Idle Process 占用 95% CPU 导致卡顿"。LagZero 在采集和分析两层都过滤掉了这个伪进程。

### 补丁七：切窗口、覆盖层不再被当成重卡顿

很多工具把"帧做出来了但上屏延迟了一下"直接报成卡顿。但切窗口、桌面合成器、录屏覆盖层都会造成短暂的上屏抖动，玩家几乎无感。LagZero 把上屏异常分成轻重两级——轻微的归到"轻微干扰"，只有连续出现或伴随掉帧的才进"卡顿报告"。

### 补丁八：报告不会因为保存数据而卡住界面

事件写入 SQLite 曾经直接在 UI 线程上做，用户切换报告时正好撞上数据库提交，界面就卡住了。LagZero 现在把所有持久化丢到后台线程，数据库开了 WAL 模式，读写不再互斥。切页时也是后台先取好数据再一次性替换，不会先清空再等。

## 一句话总结

LagZero 不只是"又一个帧率监控器"。它是一个会学习你的机器、会区分卡顿轻重、会回溯原因、会用中文给你建议的工具——把"电脑又卡了"从一个模糊的抱怨变成一个可以排查的问题。
